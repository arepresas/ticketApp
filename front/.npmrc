# Override any user/global registry to use the public npm registry.
# Keeps the repo portable across machines and CI runners.
registry=https://registry.npmjs.org/

# pnpm: hoist patterns needed for vitest + svelte testing-library
public-hoist-pattern[]=*vitest*
public-hoist-pattern[]=@testing-library/*
