<script lang="ts">
	/**
	 * Placeholder — the original Nav was removed when the global app shell
	 * header (`front/src/lib/components/layout/Header.svelte`) took over.
	 * This file stays as an empty module so any stray `import './Nav.svelte'`
	 * fails loudly at build time. Safe to delete in any cleanup PR.
	 */
</script>
