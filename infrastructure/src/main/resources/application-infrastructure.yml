spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ticketapp}
    username: ${DB_USER:ticketapp}
    password: ${DB_PASSWORD:ticketapp}
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
